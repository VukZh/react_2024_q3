import {Component, ReactNode} from "react";
import Loader from "../../../shared/loader";

class Search extends Component<ReactNode, object> {
  render() {

    return (
      <Loader/>
    );
  }
}

export default Search;
